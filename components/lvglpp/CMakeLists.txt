#Created by VisualGDB. Right-click on the component in Solution Explorer to edit properties using convenient GUI.

idf_component_register(SRCS "lvglpp.cpp"
					   INCLUDE_DIRS "."
					   REQUIRES lvgl lvgl_esp32_drivers)
